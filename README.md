# source code for https://ml.backdoors.uk

To add an entry to the table, add a YAML file to `table/entries`. (See existing
entries for the format)

To make a modification to the diagram, edit `diagram/overview.tex`.
